
这里的修改是指： 修改成内部clock的compare

1.   设置capture和compare
     CCCTL_01.17 :     0 -- compare,    1 --  capture                                                                 //修改ok     same 0
     CCCTL_01.2-0:     0  -- 关闭捕获，  1 --  上升沿捕获 ，  2 --  下降沿捕获，  3 -- both adge       //修改ok   same 0

2.  compare的输入源：
     IFCTL_01.3-0:       0 -- pwm,    2 --  捕捉  ，   3 --  内部时钟的compare用                          //修改OK  2 -> 3
     CCCTL_01.6-4:     0  -- 每个timclk，  1 --  上升沿                                                             //修改OK  1 -> 0
     
3.  输出pwm的方式：
     OCTL_01.3-0:       0 -- 源源不断输出pwm,    1+ --  来一个事件输出一次pwm （待验证）

4.  关闭pwm引脚：  （另一种方法或更改， 修改 PINCM）
     ODIS.0:       0 --CCP0的pwm,    1  --  强制低电平                                                              //修改考虑 (暂没修改)


5.  设置CCP0的方向
     CCPD.0:       0 --输入,    1  --  输出

6.  定时器使能后的计数值， 从哪个方向计数
     CTRCTL.28:        0 --LOAD,    1  --  current，    2 -- zero                                                    //修改考虑 (暂没修改)
     CTRCTL.5-4:       0 --down,    1  --  up/down，  2 -- up                                                       //修改考虑 


7. 设置或注释  PINCM，关闭pwm                                                                                          //修改OK (直接注释)

      capture：
      IOMUX->SECCFG.PINCM[24] =  4<<0 | 1<<7 | 1<<18 ;       //复用TIMA0_C0，打开引脚对外设的透传，打开输入使能 

      pwm：
      IOMUX->SECCFG.PINCM[31] =  4<<0 | 1<<7 | 0<<18 ;       //复用TIMG7_CCP0，打开引脚对外设的透传，关闭输入使能   

      compare：       
      IOMUX->SECCFG.PINCM[24] =  4<<0 | 1<<7 | 1<<18 ;       //复用TIMA0_C0，打开引脚对外设的透传，打开输入使能  


8. 读取定时器的计数值：
    CTR.15-0:   15-0  -- counter

9. 读取cc0值
    CC_01[0].15-0:   15-0  -- CC0 的门槛值

















