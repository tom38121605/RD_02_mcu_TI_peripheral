
这里的修改是指： 修改成内部clock的compare

1.   设置capture和compare  ( TIMA0->COUNTERREGS.CCCTL_01 )
     CCCTL_01.17 :     0 -- compare,    1 --  capture                                                                 //修改ok    same 0

1.   设置capture   ( TIMA0->COUNTERREGS.CCCTL_01 ) 
     CCCTL_01.2-0:     0  -- 关闭捕获，  1 --  上升沿捕获 ，  2 --  下降沿捕获，  3 -- both adge       //修改ok   same 0

2.  compare的输入源： ( TIMA0->COUNTERREGS.IFCTL_01 )
     IFCTL_01.3-0:       0 -- pwm,    2 --  捕捉  ，   3 --  内部时钟的compare用                          //修改OK  2 -> 3
     CCCTL_01.6-4:     0  -- 每个timclk，  1 --  上升沿 ，  2 --  下升沿，  3 -- both adge            //修改OK  1 -> 0
     
3.  输出pwm的方式： （ gptimer->COUNTERREGS.OCTL_01 ）
     OCTL_01.3-0:       0 -- 源源不断输出pwm,    1+ --  来一个事件输出一次pwm （待验证）

4.  设置pwm引脚电平：    ( gptimer->COMMONREGS.ODIS )
     ODIS.0:       0 --CCP0的pwm,    1  --  强制低电平                                                              //修改考虑 (暂没修改)

5.  设置CCP0的方向  ( TIMA0->COMMONREGS.CCPD )
     CCPD.0:       0 --输入,    1  --  输出

6.  定时器使能后的计数值， 从哪个方向计数    （ TIMA0->COUNTERREGS.CTRCTL ）
     CTRCTL.29-28:     0 --LOAD,    1  --  current，    2 -- zero                                                    //修改考虑 (暂没修改)
     CTRCTL.5-4:        0 --down,     1  --  up/down，  2 -- up                                                       //修改考虑 


7.  设置定时器的捕捉引脚，pwm引脚                                                                                          //修改OK (直接注释)

      capture：
      IOMUX->SECCFG.PINCM[24] =  4<<0 | 1<<7 | 1<<18 ;       //复用PB8为TIMA0_C0捕捉引脚，打开引脚对外设的透传，打开输入使能 

      pwm：
      IOMUX->SECCFG.PINCM[31] =  4<<0 | 1<<7 | 0<<18 ;       //复用PB15为TIMG7_CCP0，，打开引脚对外设的透传，关闭输入使能   

      compare：       
      IOMUX->SECCFG.PINCM[24] =  4<<0 | 1<<7 | 1<<18 ;       //复用PB8为TIMA0_C0捕捉引脚，打开引脚对外设的透传，打开输入使能  


8. 读取定时器的计数值： ( gptimer->COUNTERREGS.CTR )
    CTR.15-0:   15-0  -- counter

9. 读取cc0值   ( gptimer->COUNTERREGS.CC_01[0] )
    CC_01[0].15-0:   15-0  -- CC0 的门槛值


10. 读取load值  （ TIMA0->COUNTERREGS.LOAD ）
     LOAD.15-0:   15-0  -- tima0 的load


11. 设置定时器clock
       TIMA0->CLKSEL = 1<<3;
       TIMA0->CLKDIV = 001<<0;
       TIMA0->COMMONREGS.CPS = 3;












