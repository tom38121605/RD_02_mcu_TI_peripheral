

https://e2e.ti.com/support/microcontrollers/arm-based-microcontrollers-group/arm-based-microcontrollers/f/arm-based-microcontrollers-forum/1321497/mspm0g3507-timg6-capture-function-fail?ReplyFilter=Answers&ReplySortBy=Answers&ReplySortOrder=Descending#1


timx_timer_mode_capture_edge_capture_LP_MSPM0G3507_nortos_ticlang



=============================

Hi Sir

I use timx_timer_mode_capture_edge_capture_LP_MSPM0G3507_nortos_ticlang SDK example,

the default setting are DOWN counting, rising edge detection and down event(interrupt), but my need is up counting, falling edge detection and up event(interrupt),

so I make some modifications as below, after these modifications, I execute the program and the program enter the “DL_TIMERG_IIDX_CC0_UP” directly, even if PB2 still In high level.

In my understanding when I toggle the switch to low level, the program will enter CC0_UP interrupt, but the program always enter interrupt when I run the program.

 Is there something I've missed?

我使用了timx_timer_mode_capture_edge_capture_LP_MSPM0G3507_nortos_ticlang SDK示例，

默认设置为：降计数、上升沿检测和下降事件（中断），

但我的需求是：升计数、下降沿检测和上升事件（中断），

因此我做了如下修改，修改后执行程序，程序会直接进入“DL_TIMERG_IIDX_CC0_UP”状态，即使PB2仍处于高电平。

据我理解，当将开关切换至低电平时，程序应进入CC0_UP中断，但我运行程序时程序总是进入中断。

 我是不是漏掉了什么？


================================


I find the root of the problem. Code generated by our Sysconfig did not initialized some related register, which may cause the problem
Please change the register value in the red box to the corresponding value (the green one is the default value).

我找到了问题的根源。由我们的Sysconfig生成的代码未初始化某些相关寄存器，这可能是导致问题的原因
请将红色框中的寄存器值更改为对应的值（绿色框中的为默认值）


===============================

thanks for your help, after I manually added code, it's work.
can you ask sysconfig team fix this problem in next update?
anyway thanks again.


35 #define TIMER_CAPTURE_DURATION (CAPTURE_0_INST_LOAD_VALUE)
36 uint32_t counter_1;
37 uint32_t counter_2;
38
39 volatile bool gCheckEdgeCaptureTime;
40
41 int main(void)
42 {
43     __attribute__((unused)) volatile static uint32_t edgeCapture;
44     SYSCFG_DL_init();
45
46     TIMG6->COUNTERREGS.CCCTL_01[0]=0x20002;                         //add
47
48     NVIC_EnableIRQ(CAPTURE_0_INST_INT_IRQN);
49
50     gCheckEdgeCaptureTime = false;
51
52     DL_TimerG_startCounter(CAPTURE_0_INST);
53
54     /*
55      * LED_1 is turned on to indicate edge capture has started
56      */
57     DL_GPIO_clearPins(
58         GPIO_LEDS_PORT, GPIO_LEDS_USER_LED_1_PIN | GPIO_LEDS_USER_TEST_PIN);
59










